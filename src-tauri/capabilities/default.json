{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default permissions for the control window",
  "windows": ["control"],
  "permissions": [
    "core:default"
  ]
}
